/** Baked photo resolution used inside the Builder Card (3:4 portrait). */
export const PHOTO_TARGET = { width: 480, height: 640 };
export const PHOTO_ASPECT = PHOTO_TARGET.width / PHOTO_TARGET.height;
